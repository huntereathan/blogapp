import { FirstlettercapitalDirective } from './firstlettercapital.directive';

describe('FirstlettercapitalDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstlettercapitalDirective();
    expect(directive).toBeTruthy();
  });
});
